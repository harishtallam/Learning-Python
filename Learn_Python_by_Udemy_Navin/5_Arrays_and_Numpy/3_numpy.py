# Numpy is used for multi-dimensional arrays

from numpy import *

# When numpy is used, the array declaration will be different and one of the ways is as follows
arr = array([1, 2, 3, 4, 5])
print(arr)

# When numpy is used, the array declaration will be different and one of the ways is as follows
arr1 = array([3, 2, 3, 1, 2], 'int')
print(arr1)
